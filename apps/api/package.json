{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "dotnet watch run",
    "build": "dotnet build",
    "lint": "dotnet format --verify-no-changes",
    "format": "dotnet format"
  }
}
